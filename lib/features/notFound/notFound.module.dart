import 'package:flutter/material.dart';

class NotFoundModule extends StatelessWidget {
  const NotFoundModule({super.key});

  static const String route = '/notFound';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
