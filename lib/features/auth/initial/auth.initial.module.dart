import 'package:flutter/material.dart';

import 'auth.initial.page.dart';

class AuthInitialModule extends StatelessWidget {
  const AuthInitialModule({super.key});

  static const String route = '/auth';

  @override
  Widget build(BuildContext context) => const AuthInitialPage();
}
